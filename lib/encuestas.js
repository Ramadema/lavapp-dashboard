// Fuente de datos de la encuesta. Next resuelve el JSON en build, asi que viaja
// dentro del bundle de la funcion sin depender del filesystem.
import encuestas from "@/data/encuestas.json";

export default encuestas;
